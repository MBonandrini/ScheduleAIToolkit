"use strict";
const Core=window.parent.ProjectControlsCore, content=document.getElementById("settingsContent"), status=document.getElementById("settingsStatus");
let page="general";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
const origin=()=>{try{return window.parent.location.origin||location.origin}catch(_){return location.origin}};
function notify(){try{window.parent.postMessage({type:"pc-ai-config-changed"},"*");window.parent.document.querySelectorAll("iframe.tool-frame").forEach(f=>{try{f.contentWindow.postMessage({type:"pc-ai-config-changed"},"*")}catch(_){}})}catch(_){}}
document.querySelectorAll(".settings-tabs button").forEach(b=>b.onclick=()=>{page=b.dataset.page;document.querySelectorAll(".settings-tabs button").forEach(x=>x.classList.toggle("active",x===b));render()});
function general(){const ai=Core.ai.status(),omni=Core.ai.config(),ollama=Core.ai.ollamaConfig();return `<div class="grid"><section class="card"><div class="card-head">Appearance</div><div class="card-body"><div class="field"><label>Theme</label><select id="themeSelect"><option value="light">Light</option><option value="dark">Dark</option></select></div><div class="actions"><button id="applyTheme" class="btn primary">Apply theme</button></div></div></section><section class="card"><div class="card-head">Unified AI status</div><div class="card-body"><div class="rows"><div class="row"><span>Selected option</span><b>${esc(Core.ai.preferredLabel())}</b></div><div class="row"><span>Loaded engine</span><b>${esc(ai.engine||"idle")}</b></div><div class="row"><span>Runtime model</span><b>${esc(ai.model||"—")}</b></div></div><div class="actions"><button id="releaseAI" class="btn">Unload browser model</button><button id="goAI" class="btn primary">Open AI configuration</button></div></div></section><section class="card wide"><div class="card-head">Saved connection configuration</div><div class="card-body"><div class="rows"><div class="row"><span>OmniRoute URL</span><b>${esc(omni.baseUrl)}</b></div><div class="row"><span>Ollama URL</span><b>${esc(ollama.baseUrl)}</b></div><div class="row"><span>Ollama chat model</span><b>${esc(ollama.model||"Not selected")}</b></div><div class="row"><span>Ollama embedding model</span><b>${esc(Core.ai.ollamaEmbeddingModel()||"Keyword-only")}</b></div></div><div class="actions"><button id="clearAIConfig" class="btn danger">Reset AI settings</button></div><div id="resetDiag" class="diag">Resetting removes saved endpoints/model choices from this browser. It does not uninstall OmniRoute, Ollama or any model.</div></div></section></div>`}
function ai(){const omni=Core.ai.config(),oll=Core.ai.ollamaConfig(),preferred=Core.ai.preferred();const opts=Core.ai.catalog.filter(x=>!x.disabled).map(x=>`<option value="${esc(x.value)}" ${x.value===preferred?'selected':''}>${esc(x.label)}</option>`).join("");return `<div class="grid"><section class="card wide emphasis"><div class="card-head">One AI selection for the entire suite</div><div class="card-body"><div class="field"><label>Active AI engine / route</label><select id="globalAISelect">${opts}</select></div><div class="note" style="margin-top:10px"><b>This selection is global.</b> Contract Manager, Drawing Measurement, Schedule Assessment, Risk Analysis, Claims & Forensics, Schedule Builder and NotebookLM+ all use this choice. Individual modules can no longer override it.</div><div class="actions"><button id="saveGlobalAI" class="btn primary">Use this AI everywhere</button><button id="testSelectedAI" class="btn">Test selected AI</button></div><div id="globalDiag" class="diag">Current: ${esc(Core.ai.preferredLabel())}</div></div></section><section class="card"><div class="card-head">OmniRoute connection</div><div class="card-body"><div class="field"><label>API base URL</label><input id="omniUrl" value="${esc(omni.baseUrl)}"></div><div class="field" style="margin-top:8px"><label>Endpoint key — optional, session only</label><input id="omniKey" type="password" autocomplete="off" placeholder="Leave blank for keyless local access"></div><div class="actions"><button id="saveOmni" class="btn">Save connection</button><button id="testOmni" class="btn primary">Test OmniRoute</button></div><div id="omniDiag" class="diag">Default: http://localhost:20128/v1</div></div></section><section class="card"><div class="card-head">Ollama connection</div><div class="card-body"><div class="field"><label>Ollama host</label><input id="ollamaUrl" value="${esc(oll.baseUrl)}"></div><div class="form-grid" style="margin-top:8px"><div class="field"><label>Chat model</label><select id="ollamaModel"><option value="${esc(oll.model)}">${esc(oll.model||"Detect models first")}</option></select></div><div class="field"><label>Embedding model for NotebookLM+</label><select id="embeddingModel"><option value="${esc(Core.ai.ollamaEmbeddingModel())}">${esc(Core.ai.ollamaEmbeddingModel()||"Keyword-only")}</option></select></div></div><div class="actions"><button id="detectOllama" class="btn">Detect models</button><button id="testOllama" class="btn primary">Test & Save</button></div><div id="ollamaDiag" class="diag">Default: http://localhost:11434</div></div></section><section class="card wide"><div class="card-head">Browser / local model note</div><div class="card-body"><div class="note">Browser CPU/WebGPU models also use the same global selector. NotebookLM+ uses them for answer generation through the shared suite AI runtime; semantic vector retrieval falls back to keyword retrieval unless an Ollama embedding model is selected with Ollama as the global engine.</div></div></section></div>`}
function tutorial(){return `<div class="tutorial-grid"><section class="card"><div class="card-head">Quick start</div><div class="card-body"><ol><li>Open <b>Unified AI Configuration</b>.</li><li>Choose one engine for the entire suite.</li><li>Configure and test its local endpoint if required.</li><li>Open any module. It will automatically use the global choice.</li></ol><div class="note">Deterministic project-controls calculations do not require AI. AI is used for explanation, narrative and conversational analysis above verified results.</div></div></section><section class="card"><div class="card-head">Ollama setup</div><div class="card-body"><p>Install and start Ollama. Default host: <code>http://localhost:11434</code>.</p><p>Install a chat model, for example:</p><pre><code>ollama run gemma3</code></pre><p><a class="btn primary" href="../notebooklmplus/downloads/NotebookLMPlus-Ollama-Setup.zip" download>Download Windows Ollama setup helper</a></p><p>For NotebookLM+ semantic retrieval, also install an embedding model such as:</p><pre><code>ollama pull embeddinggemma</code></pre><div class="note warning">For a GitHub Pages deployment, set <b>OLLAMA_ORIGINS</b> to the exact site origin and restart Ollama:<br><code>${esc(origin())}</code></div></div></section><section class="card"><div class="card-head">OmniRoute setup</div><div class="card-body"><p>Default API endpoint: <code>http://localhost:20128/v1</code>.</p><p>In OmniRoute security/CORS settings, allow the exact site origin:</p><pre><code>${esc(origin())}</code></pre><p>Normal local keyless operation can leave the endpoint key blank. Choose an OmniRoute route such as <b>Auto</b> in Unified AI Configuration.</p></div></section><section class="card"><div class="card-head">Browser AI</div><div class="card-body"><p>Browser CPU/WASM and WebGPU models load only when selected. They require no Ollama or OmniRoute service but can consume substantial RAM/VRAM.</p><p>Smaller models are preferable on normal business laptops.</p></div></section><section class="card wide"><div class="card-head">Troubleshooting</div><div class="card-body"><table><tr><th>Issue</th><th>Check</th></tr><tr><td>Ollama unreachable</td><td>Service running, localhost:11434, OLLAMA_ORIGINS and browser local-network permission.</td></tr><tr><td>OmniRoute unreachable</td><td>Service running, localhost:20128/v1, CORS origin, endpoint authentication and provider availability.</td></tr><tr><td>NotebookLM+ has no semantic retrieval</td><td>Use Ollama globally and choose an installed embedding model, or continue with keyword retrieval.</td></tr><tr><td>Browser model is slow</td><td>Choose a smaller model, Ollama or OmniRoute.</td></tr></table><div class="note warning" style="margin-top:10px">Do not place long-lived paid-provider API keys in public GitHub Pages JavaScript.</div></div></section></div>`}
function render(){content.innerHTML=page==="ai"?ai():page==="tutorial"?tutorial():general();bind()}
function setPage(next){page=next;document.querySelectorAll(".settings-tabs button").forEach(x=>x.classList.toggle("active",x.dataset.page===next));render()}
function bind(){
 document.getElementById("themeSelect")&&(document.getElementById("themeSelect").value=window.parent.document.documentElement.dataset.theme||"light");
 document.getElementById("applyTheme")?.addEventListener("click",()=>{const t=document.getElementById("themeSelect").value;try{localStorage.setItem("projectControlsTheme",t)}catch(_){};window.parent.document.documentElement.dataset.theme=t;document.documentElement.dataset.theme=t;window.parent.document.dispatchEvent(new CustomEvent("pc-theme-change",{detail:{theme:t}}));status.textContent=`${t} theme applied`});
 document.getElementById("releaseAI")?.addEventListener("click",async()=>{await Core.ai.release();status.textContent="AI runtime unloaded";render()});
 document.getElementById("goAI")?.addEventListener("click",()=>setPage("ai"));
 document.getElementById("clearAIConfig")?.addEventListener("click",()=>{try{["projectControlsOmniRouteBaseUrl","projectControlsOllamaBaseUrl","projectControlsOllamaModel","projectControlsOllamaEmbeddingModel","projectControlsSharedAIModel"].forEach(k=>localStorage.removeItem(k));sessionStorage.removeItem("projectControlsOmniRouteEndpointKey")}catch(_){};Core.ai.configure({baseUrl:"http://localhost:20128/v1",endpointKey:""});Core.ai.configureOllama({baseUrl:"http://localhost:11434",model:""});Core.ai.configureOllamaEmbedding("");Core.ai.setPreferred("omniroute:auto");notify();document.getElementById("resetDiag").innerHTML='<span class="ok">✓ AI settings reset.</span>';status.textContent="AI settings reset"});
 document.getElementById("saveGlobalAI")?.addEventListener("click",async()=>{const v=document.getElementById("globalAISelect").value;Core.ai.setPreferred(v);await Core.ai.release();notify();document.getElementById("globalDiag").innerHTML=`<span class="ok">✓ ${esc(Core.ai.preferredLabel())} will now be used throughout the suite.</span>`;status.textContent="Unified AI selection saved"});
 document.getElementById("testSelectedAI")?.addEventListener("click",async e=>{const b=e.currentTarget,d=document.getElementById("globalDiag"),v=document.getElementById("globalAISelect").value;b.disabled=true;d.textContent="Testing selected AI…";try{Core.ai.setPreferred(v);await Core.ai.release();const r=await Core.ai.ensure(v);const out=await Core.ai.run([{role:"user",content:"Reply with exactly OK"}],{temperature:0,max_tokens:8});d.innerHTML=`<span class="ok">✓ ${esc(r.label||Core.ai.preferredLabel())} responded: ${esc(out?.choices?.[0]?.message?.content||"OK")}</span>`;notify()}catch(err){d.innerHTML=`<span class="bad">✕ ${esc(err?.message||err)}</span>`}finally{b.disabled=false}});
 document.getElementById("saveOmni")?.addEventListener("click",()=>{Core.ai.configure({baseUrl:document.getElementById("omniUrl").value,endpointKey:document.getElementById("omniKey").value});document.getElementById("omniKey").value="";document.getElementById("omniDiag").innerHTML='<span class="ok">✓ OmniRoute connection saved.</span>';notify()});
 document.getElementById("testOmni")?.addEventListener("click",async e=>{const b=e.currentTarget,d=document.getElementById("omniDiag");b.disabled=true;d.textContent="Testing OmniRoute…";try{Core.ai.configure({baseUrl:document.getElementById("omniUrl").value,endpointKey:document.getElementById("omniKey").value});const r=await Core.ai.testConnection();d.innerHTML=`<span class="ok">✓ Connected. ${r.modelCount} route(s); tested ${esc(r.testedModel)}.</span>`}catch(err){d.innerHTML=`<span class="bad">✕ ${esc(err?.message||err)}</span>`}finally{document.getElementById("omniKey").value="";b.disabled=false}});
 document.getElementById("detectOllama")?.addEventListener("click",async e=>{const b=e.currentTarget,d=document.getElementById("ollamaDiag"),chat=document.getElementById("ollamaModel"),emb=document.getElementById("embeddingModel");b.disabled=true;d.textContent="Detecting models…";try{Core.ai.configureOllama({baseUrl:document.getElementById("ollamaUrl").value});const m=await Core.ai.listOllamaModels();const opts=m.map(x=>`<option value="${esc(x.name)}">${esc(x.name)}${x.parameterSize?` · ${esc(x.parameterSize)}`:""}</option>`).join("");chat.innerHTML=opts||'<option value="">No models installed</option>';const current=Core.ai.ollamaConfig().model;if([...chat.options].some(x=>x.value===current))chat.value=current;emb.innerHTML='<option value="">Keyword-only</option>'+opts;const em=Core.ai.ollamaEmbeddingModel();if([...emb.options].some(x=>x.value===em))emb.value=em;d.innerHTML=m.length?`<span class="ok">✓ ${m.length} model(s) detected.</span>`:'<span class="warn">Ollama is reachable but no models are installed.</span>'}catch(err){d.innerHTML=`<span class="bad">✕ ${esc(err?.message||err)}</span>`}finally{b.disabled=false}});
 document.getElementById("testOllama")?.addEventListener("click",async e=>{const b=e.currentTarget,d=document.getElementById("ollamaDiag");b.disabled=true;d.textContent="Testing Ollama…";try{const r=await Core.ai.testOllamaConnection({baseUrl:document.getElementById("ollamaUrl").value,model:document.getElementById("ollamaModel").value});if(!r.ok){d.innerHTML=`<span class="warn">${esc(r.message)}</span>`;return}Core.ai.configureOllama({baseUrl:document.getElementById("ollamaUrl").value,model:r.selectedModel});Core.ai.configureOllamaEmbedding(document.getElementById("embeddingModel").value);d.innerHTML=`<span class="ok">✓ Ollama ready: ${esc(r.selectedModel)}.</span>`;notify()}catch(err){d.innerHTML=`<span class="bad">✕ ${esc(err?.message||err)}</span>`}finally{b.disabled=false}});
}
render();
