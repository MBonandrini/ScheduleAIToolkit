"use strict";
const Core=window.parent.ProjectControlsCore,content=document.getElementById("tutorialContent"),summary=document.getElementById("providerSummary");let page="start";
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}
function origin(){try{return window.parent.location.origin||location.origin}catch(_){return location.origin}}
document.querySelectorAll("#tutorialTabs button").forEach(b=>b.onclick=()=>{page=b.dataset.page;document.querySelectorAll("#tutorialTabs button").forEach(x=>x.classList.toggle("active",x===b));render()});
function start(){return `<div class="hero"><div class="card"><div class="card-head">Choose an AI path</div><div class="card-body"><div class="step"><div class="step-num">1</div><div><h3>Deterministic analysis first</h3><p>Parsing, comparisons, DCMA, float, risk and forensic calculations do not require AI.</p></div></div><div class="step"><div class="step-num">2</div><div><h3>Choose a provider</h3><p>OmniRoute for automatic routing, Ollama for local privacy, Browser AI as fallback.</p></div></div><div class="step"><div class="step-num">3</div><div><h3>Test before use</h3><p>Verify the provider before running AI chat or narrative generation.</p></div></div></div></div><div class="card"><div class="card-head">Recommended order</div><div class="card-body"><table><tr><th>Priority</th><th>Provider</th><th>Best use</th></tr><tr><td>1</td><td>OmniRoute Auto</td><td>Automatic routing</td></tr><tr><td>2</td><td>Ollama</td><td>Private local AI</td></tr><tr><td>3</td><td>Browser AI</td><td>No-install fallback</td></tr></table><div class="note" style="margin-top:10px">AI explains verified results; deterministic schedule calculations remain authoritative.</div></div></div></div>`}
function ollama(){const c=Core.ai.ollamaConfig();return `<div class="hero"><div class="card"><div class="card-head">Connect local Ollama</div><div class="card-body"><div class="form-grid"><div class="field"><label>Ollama host</label><input id="ou" value="${esc(c.baseUrl)}"></div><div class="field"><label>Installed model</label><select id="om"><option value="">Run detection first</option></select></div></div><div class="actions"><button class="btn" id="od">Detect models</button><button class="btn primary" id="ot">Test & Save</button></div><div id="og" class="diag">No connection test has been run.</div></div></div><div class="card"><div class="card-head">GitHub Pages browser access</div><div class="card-body"><p>Local Ollama requires no API key, but the website origin must be allowed.</p><div class="note warning" style="margin-top:9px">Set <b>OLLAMA_ORIGINS</b> to:<span class="code">${esc(origin())}</span></div></div></div></div><div class="card"><div class="card-head">Windows setup</div><div class="card-body"><div class="step"><div class="step-num">1</div><div><h3>Install and start Ollama</h3><p>Default local service: http://localhost:11434</p></div></div><div class="step"><div class="step-num">2</div><div><h3>Install a model</h3><span class="code">ollama run gemma3</span></div></div><div class="step"><div class="step-num">3</div><div><h3>Allow this site</h3><p>Quit Ollama, create the Windows user environment variable below, restart Ollama.</p><span class="code">OLLAMA_ORIGINS=${esc(origin())}</span></div></div><div class="step"><div class="step-num">4</div><div><h3>Detect and test</h3><p>Click Detect models, select one, then Test & Save.</p></div></div><div class="step"><div class="step-num">5</div><div><h3>Select Ollama in any AI dropdown</h3><p>Choose “Ollama — selected local model”.</p></div></div></div></div><div class="note warning" style="margin-top:10px">Large Ollama models may use several GB of RAM and GPU memory.</div>`}
function omni(){const c=Core.ai.config();return `<div class="hero"><div class="card"><div class="card-head">OmniRoute connection</div><div class="card-body"><div class="field"><label>API base URL</label><input id="mu" value="${esc(c.baseUrl)}"></div><div class="actions"><button class="btn primary" id="mt">Test OmniRoute</button></div><div id="mg" class="diag">No connection test has been run.</div></div></div><div class="card"><div class="card-head">Browser origin</div><div class="card-body"><p>For local OmniRoute, allow this origin in OmniRoute CORS settings.</p><span class="code">${esc(origin())}</span></div></div></div>`}
function browser(){return `<div class="cards"><div class="provider-card"><h3>Qwen2.5 0.5B CPU/WASM</h3><p>Approximately 0.8–1.5 GB RAM.</p></div><div class="provider-card"><h3>WebGPU 1.5B–3B</h3><p>Better quality but several GB of GPU/system memory.</p></div><div class="provider-card"><h3>8B models</h3><p>Higher quality but unsuitable for many business laptops.</p></div></div><div class="card"><div class="card-head">Browser-local models</div><div class="card-body"><p>Select one from any AI dropdown. Models load only on demand and show a memory warning first.</p></div></div>`}
function trouble(){return `<div class="card"><div class="card-head">Troubleshooting</div><div class="card-body"><table><tr><th>Symptom</th><th>Likely cause</th><th>Action</th></tr><tr><td>Ollama unreachable</td><td>Stopped service or CORS/local-network block</td><td>Confirm localhost:11434 and set OLLAMA_ORIGINS.</td></tr><tr><td>Ollama has no models</td><td>No model installed</td><td>Run/pull a model, then detect again.</td></tr><tr><td>OmniRoute fails</td><td>Service, CORS, provider, quota or endpoint auth</td><td>Use AI Settings diagnostics.</td></tr><tr><td>Browser AI is slow</td><td>Insufficient RAM/VRAM</td><td>Use Qwen 0.5B, Ollama or OmniRoute.</td></tr></table><div class="note warning" style="margin-top:10px">Do not embed paid-provider API keys in public GitHub Pages JavaScript.</div></div></div>`}
function render(){content.innerHTML=({start,ollama,omni,browser,trouble}[page]||start)();bind()}
function bind(){document.getElementById("od")?.addEventListener("click",detect);document.getElementById("ot")?.addEventListener("click",testO);document.getElementById("mt")?.addEventListener("click",testM)}
async function detect(){const b=document.getElementById("od"),g=document.getElementById("og"),s=document.getElementById("om"),u=document.getElementById("ou").value.trim();b.disabled=true;g.textContent="Detecting installed Ollama models…";try{Core.ai.configureOllama({baseUrl:u});const m=await Core.ai.listOllamaModels();s.innerHTML=m.length?m.map(x=>`<option value="${esc(x.name)}">${esc(x.name)}${x.parameterSize?` · ${esc(x.parameterSize)}`:""}${x.quantization?` · ${esc(x.quantization)}`:""}</option>`).join(""):'<option value="">No models installed</option>';const saved=Core.ai.ollamaConfig().model;if([...s.options].some(x=>x.value===saved))s.value=saved;g.innerHTML=m.length?`<span class="ok">✓ Ollama reached. ${m.length} model(s) detected.</span>`:`<span class="warn">Ollama is reachable but no models are installed.</span>`}catch(e){g.innerHTML=`<span class="bad">✕ ${esc(e?.message||e)}</span>`}finally{b.disabled=false}}
async function testO(){const b=document.getElementById("ot"),g=document.getElementById("og"),u=document.getElementById("ou").value.trim(),m=document.getElementById("om").value;b.disabled=true;g.textContent="Testing Ollama chat…";try{const r=await Core.ai.testOllamaConnection({baseUrl:u,model:m});if(!r.ok){g.innerHTML=`<span class="warn">${esc(r.message)}</span>`;return}Core.ai.configureOllama({baseUrl:u,model:r.selectedModel});g.innerHTML=`<span class="ok">✓ Ollama connected. Model: ${esc(r.selectedModel)}. Response: ${esc(r.content)}</span>`;summary.textContent=`Ollama ready · ${r.selectedModel}`}catch(e){g.innerHTML=`<span class="bad">✕ ${esc(e?.message||e)}</span>`;summary.textContent="Ollama connection issue"}finally{b.disabled=false}}
async function testM(){const b=document.getElementById("mt"),g=document.getElementById("mg"),u=document.getElementById("mu").value.trim();b.disabled=true;g.textContent="Testing OmniRoute…";try{Core.ai.configure({baseUrl:u});const r=await Core.ai.testConnection();g.innerHTML=`<span class="ok">✓ OmniRoute connected. ${r.modelCount} route(s), test route ${esc(r.testedModel)}.</span>`;summary.textContent="OmniRoute ready"}catch(e){g.innerHTML=`<span class="bad">✕ ${esc(e?.message||e)}</span>`;summary.textContent="OmniRoute connection issue"}finally{b.disabled=false}}
render();